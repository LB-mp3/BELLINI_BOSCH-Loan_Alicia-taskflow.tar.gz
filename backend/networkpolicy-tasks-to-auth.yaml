apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-tasks-to-auth
  namespace: taskflow-backend
spec:
  podSelector:
    matchLabels:
      app: auth
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: tasks
